import React, { JSX } from 'react';

const TypedJsxTest = (): JSX.Element => {
  const element: JSX.Element = (
    <div>
      <p>Typed JSX Test</p>
    </div>
  );
  return element;
};

export default TypedJsxTest;
